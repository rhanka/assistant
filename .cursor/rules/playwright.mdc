---
description: Rules to pilot Cursor Web Agents via Playwright
alwaysApply: true
tags: [playwright, cursor]
---

# PLAYWRIGHT
- Use a centralized helper in `packages/integrations/cursor`.
- First step: `page.goto('https://cursor.com/agents')`; handle login manually once.
- Always document: which page, which action, which selector.
- Default to **dry-run**; require "⚠ approval" for critical actions.
