{
  "name": "@repo/workers",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "node --loader ts-node/esm --watch src/index.ts",
    "build": "tsc -p tsconfig.json",
    "start": "node dist/index.js",
    "test": "vitest run",
    "test:unit": "vitest run tests/unit/",
    "test:integration": "vitest run tests/integration/",
    "test:integration:ai": "vitest run tests/integration/ai.spec.ts",
    "test:integration:api": "vitest run tests/integration/api.spec.ts"
  },
  "dependencies": {
    "bullmq": "^5.7.0"
  },
  "devDependencies": {
    "typescript": "^5.5.4",
    "ts-node-dev": "^2.0.0",
    "vitest": "^3.2.4",
    "@types/node": "^20.14.10"
  }
}
